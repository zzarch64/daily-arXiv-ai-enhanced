<div id=toc></div>

# Table of Contents

- [cs.AR](#cs.AR) [Total: 15]


<div id='cs.AR'></div>

# cs.AR [[Back]](#toc)

### [1] [KerColle: Unlocking Fine-Grained GPU Concurrency in Vision-Language-Action Models](https://arxiv.org/abs/2609.22335)
*Anna Li,Christina Giannoula,Nandita Vijaykumar*

Main category: cs.AR

TL;DR: 针对VLA推理中GPU利用率低的问题，提出轻量级GPU调度框架KerColle，通过在线SM利用率与核资源需求动态协同调度核，缓解队头阻塞并实现资源互补，仿真中吞吐提升最高达28%。


<details>
  <summary>Details</summary>
Motivation: VLA模型是高吞吐机器人控制的基础模型，其推理需满足机器人控制频率要求。VLA由VLM与动作头两阶段组成，可解耦并跨独立机器人请求异步并发执行。但对四个SOTA VLA的表征显示，GPU利用率严重不足：GPU的CTA调度器无法充分重叠这两个独立阶段，根因是硬件线程块分派器中的队头阻塞（head-of-line blocking）。且既有调度框架无法应对VLA并发挑战：不同请求的各阶段由大量核组成且并行组合时刻变化，静态或提前调度失效；动作头多为短时核且数量众多，无在线剖析或抢占机制的空间。

Method: 提出KerColle：一种轻量级GPU调度框架。利用在线SM利用率与单个核的资源需求，智能且动态地协同调度核，以高效重叠两个执行阶段，具体通过(1)缓解队头阻塞，(2)将资源需求互补的核协同调度。

Result: 在两种GPU架构、四种SOTA VLA模型上的仿真表明，KerColle带来最高28%的吞吐提升。

Conclusion: 通过对VLA推理的系统表征定位到硬件线程块分派的队头阻塞问题，并证明动态协同调度（在线SM利用率+核资源互补）是无需在线剖析或抢占即可缓解该瓶颈的有效途径，仿真验证了显著吞吐收益。

Abstract: Vision-Language-Action (VLA) models have emerged as foundational models for next-generation robotics. High VLA inference throughput is critical for meeting the control-rate requirements of robots. VLA models comprise two phases, a vision-language model (VLM) and an action head, that can be decoupled and executed asynchronously and concurrently across independent robot requests. Through a detailed characterization of four state-of-the-art VLAs, we observe that GPUs are severely underutilized in VLA inference as the Cooperative Thread Array (CTA) scheduler of GPU is unable to fully overlap the two independent phases of VLA execution. We identify that this inefficiency is caused by head-of-line blocking in the hardware thread-block dispatcher.
  We demonstrate that prior scheduling frameworks do not address the challenges posed by VLA concurrency. First, the independent phases across different robot requests each comprise numerous kernels, and at any given time, there are different combinations of kernels that are executed in parallel. This makes static or ahead-of-time scheduling policies largely ineffective. Second, many of the action-head operators are short-running kernels and there are numerous such kernels. This leaves no headroom for online profiling or preemption-based mechanisms. To address these challenges, we present KerColle, a lightweight GPU scheduling framework that leverages online Streaming Multiprocessor (SM) utilization and individual kernel resource requirements to intelligently and dynamically co-schedule kernels to efficiently overlap the two phases of execution by (1) mitigating head-of-line blocking, and (2) co-scheduling kernels with complementary resource requirements. We demonstrate in simulation, across two GPU architectures, for 4 state-of-the-art VLA models, that KerColle delivers throughput gains of up to $28\%$.

</details>


### [2] [Design-Space Exploration of Post-Quantum Cryptography Acceleration on an Open-Source RISC-V GPGPU](https://arxiv.org/abs/2609.22343)
*Hossam Hassan,Marc Xander Makkes,Chae Eun Lee,Hyungmin Yoon*

Main category: cs.AR

TL;DR: 本文首次在开源 Vortex RISC-V GPGPU 上集成了后量子密码加速单元（PCU），实现面向 Kyber 与 Dilithium 模数的 256 点 NTT，逻辑单元数最小，并在仿真与综合中验证了显著加速效果。


<details>
  <summary>Details</summary>
Motivation: 后量子密码（PQC）加速已在 CPU 和 FPGA 上得到研究，但主导高吞吐计算的 GPGPU 尚未被探索用于将 PQC 硬件作为功能单元集成，本文旨在填补这一空白。

Method: 在开源 Vortex RISC-V GPGPU 上设计并集成 Vortex 后量子密码单元（PCU），该单元以 134 个逻辑单元（73 LUT4 + 61 CCU2C）实现 Kyber（q=3329）和 Dilithium（q=8380417）模数下的 256 点 NTT。通过 Lattice ECP5 与 Xilinx Artix-7 进行综合与布局布线，并在 Vortex SimX 与 ASAP7 7nm 工艺下评估。

Result: PCU 为已报道逻辑单元数最小的 NTT 加速器，比次小设计小 2.8 倍；在 Lattice ECP5 上达 228.78 MHz，在 Xilinx Artix-7 布局布线后达 299.8 MHz，通过 7 个套件共 253 项功能测试。在 Vortex SimX 上相对 GPU ALU 执行实现 42.6 倍加速，RTL 上预计约 36 倍加速。ASAP7 7nm 综合后 NTT 数据通路占 96.53 um^2，频率 702 MHz。

Conclusion: 该工作为将 PQC 硬件加速集成到通用并行计算中奠定了基础。

Abstract: Post-quantum cryptography (PQC) acceleration has been studied for CPUs and FPGAs, yet GPGPUs - dominating high-throughput computing - remain unexplored for PQC hardware integration as a functional unit. We present what is, to our knowledge, the first such integration: the Vortex Post-Quantum Crypto Unit (PCU) on the open-source Vortex RISC-V GPGPU. The PCU implements a 256-point NTT over Kyber (q=3329) and Dilithium (q=8380417) moduli in 134 logic cells (73 LUT4 + 61 CCU2C) - the smallest reported NTT accelerator by logic-cell count, 2.8x smaller than the next smallest design. It achieves 228.78 MHz on Lattice ECP5 and 299.8 MHz place-and-route on Xilinx Artix-7, and passes 253 functional tests across 7 suites. On Vortex SimX, it achieves 42.6x acceleration over GPU ALU execution, projecting to roughly 36x speedup on RTL. Synthesized to ASAP7 7nm, the NTT datapath occupies 96.53 um^2 (post-route) at 702 MHz. This work establishes a foundation for integrating PQC hardware acceleration into general-purpose parallel computing.

</details>


### [3] [Verification Reward Model for Reinforcement Learning in Chip Design Verification](https://arxiv.org/abs/2609.22347)
*Shashank Chaurasia*

Main category: cs.AR

TL;DR: 本文提出验证奖励模型（VRM）框架，用于训练语言模型创建和修复芯片验证工件，核心是版本化验证契约，将编译、仿真、形式、变异、覆盖率和专家评审证据转为可审计记录，确定性验收检查保持在模型之外。


<details>
  <summary>Details</summary>
Motivation: 芯片验证工件（如测试平台、激励、断言）的创建与修复依赖高成本证据（仿真、形式验证、覆盖率等），直接作为训练信号昂贵且不可靠；需要一种将多源验证证据转化为可审计、任务条件化奖励的框架，以支撑语言模型在验证领域的可扩展训练与评估。

Method: 提出VRM框架：以版本化验证契约绑定需求、允许激励、观测边界、参考行为、评估预算与验收标准；将编译器、仿真器、形式、变异、覆盖率、专家评审证据转为可审计记录；确定性验收检查外置。学习型结果模型从规范、生成工件及显式掩码的部分证据预测昂贵未来证据；语义评论器识别有证据支撑的弱点；确定性奖励合成器将质量向量转为任务条件化训练奖励。扩展包括配对干净/故障干预、边际故障发现奖励、不确定性感知评估调度、隔离式跨域学习循环。

Result: 本文为立场论文，仅描述框架与拟测试实验，报告在一个小型开源工具兼容基准上、使用轻量模型的首个实验设想，完整UVM能力通过特征特定资格认证纳入；未报告任何训练、EDA或硅片结果。

Conclusion: VRM将验证工件生成与修复形式化为受契约约束、证据驱动、奖励可审计的学习问题，并给出评估重点（独立验证的故障检测、误报、跨设计族泛化、达到指定质量水平的总成本）；当前仅为框架与实验设计，尚待实证验证。

Abstract: We propose a Verification Reward Model (VRM) framework for training language models to create and repair chip verification artifacts. The central object is a versioned verification contract that binds requirements, permissible stimulus, observation boundaries, reference behavior, evaluation budgets, and acceptance criteria. Compiler, simulator, formal, mutation, coverage, and expert-review evidence are converted into auditable records. Deterministic acceptance checks remain outside the learned model. A learned outcome model predicts expensive future evidence from the specification, the generated artifact, and explicitly masked partial evidence; a semantic critic identifies evidence-supported weaknesses; a deterministic reward composer translates the resulting quality vector into task-conditioned training rewards. Extensions include paired clean/fault interventions, marginal fault-discovery rewards, uncertainty-aware evaluation scheduling, and a quarantined cross-domain learning loop. Evaluation emphasizes independently validated fault detection, false alarms, generalization across design families, and total cost to reach a specified quality level. We describe a first experiment on a small, open-tool-compatible benchmark with lightweight models, with full UVM capability admitted through feature-specific qualification. This is a position paper: we specify the framework and the experiments that would test it, and we report no training, EDA, or silicon results.

</details>


### [4] [UniCASE: A Unified 16-bit Floating-Point Format with Criticality-Aware Selective ECC for Efficient DNN Protection](https://arxiv.org/abs/2609.22590)
*Amna Hassan,Semeen Rehman*

Main category: cs.AR

TL;DR: 本文提出 UniCASE，一种统一的 16 位浮点格式，通过联合优化数据表示与错误保护，在降低编解码开销的同时提升 DNN 的软错误鲁棒性。


<details>
  <summary>Details</summary>
Motivation: 软错误会破坏 DNN 参数导致精度下降。传统 ECC 保护强但带来额外奇偶存储与计算开销；嵌入式奇偶格式虽降低存储成本，却未在减少计算开销的同时优化保护效果。

Method: UniCASE 识别 FP64、FP32、FP16 与 BFloat16 中可映射到统一表示的稳定块；基于位级关键性分析，采用选择性 ECC，为不同数据位分配差异化保护等级。

Result: 实验表明，UniCASE 将编解码开销最多降低 30%，模型精度保持在 FP32 基线 1% 以内，且软错误鲁棒性显著优于现有嵌入式奇偶方法。

Conclusion: UniCASE 通过统一 16 位浮点格式联合优化数据表示与选择性错误保护，在开销、精度与可靠性之间取得优于现有方案的平衡。

Abstract: Soft errors are an increasing reliability concern for Deep Neural Network execution because they can corrupt parameters, leading to accuracy degradation. While conventional ECC offers strong fault protection, it incurs additional parity storage and computational overhead. Embedded-parity formats reduce storage cost by reusing the least-significant bits, but they do not optimize protection while reducing computational overhead. We propose UniCASE, a unified 16-bit floating-point (FP) format that jointly optimizes data representation and error protection for reliable DNN execution. It identifies stable blocks across FP64, FP32, FP16, and BFloat16 that can be mapped into a unified representation. Based on bit-level criticality analysis, UniCASE uses selective ECC that assigns distinct levels of protection to different data bits according to their resilience against soft errors. Experimental results show that UniCASE reduces encoder/decoder cost by up to 30%, preserves model accuracy within 1% of the FP32 baseline, and provides significantly stronger soft error resilience than existing embedded-parity methods.

</details>


### [5] [Presage: Prefetch Search via Agent-Guided Experiments](https://arxiv.org/abs/2609.22636)
*Matthew Giordano,Parthasarathy Ranganathan,Baris Kasikci,Akanksha Jain*

Main category: cs.AR

TL;DR: Presage 利用大语言模型的语义推理能力自动插入软件预取指令，通过提案智能体、长程优化智能体和组合智能体协同工作，在81个工作负载上实现平均10%的几何均值性能提升，并在大规模代码库场景下显著优于既有SOTA方法。


<details>
  <summary>Details</summary>
Motivation: 软件预取虽能缓解缓存缺失延迟，但手动插入耗时且难以预测，对微架构和代码变更高度敏感。现有编译器与FDO方法依赖难以泛化的启发式规则，且未考虑大规模代码库中出现的预取共依赖与干扰等新挑战。作者认为对程序的语义理解与推理是插入有效软件预取的关键缺失环节。

Method: 提出 Presage 系统，借助大语言模型的语义推理插入有效软件预取。系统通过定制智能体框架处理工作负载：首先由提案智能体结合性能指标识别有潜力的预取候选；随后多个长程优化智能体在循环中迭代优化候选预取；最后由组合智能体负责将单独有效的预取进行组合，以应对预取共依赖与干扰问题。

Result: 在81个工作负载上，Presage 插入的预取实现几何均值10%的性能提升，每个工作负载探索数十种预取方案；在 SPEC CPU 2026 上实现2.7%的运行时间缩减，而此前SOTA方法在该场景失败；在其评估套件上，Presage 的几何均值运行时间改进达17%，优于SOTA的10%。

Conclusion: 语义理解与推理是插入有效软件预取的关键要素。基于LLM的智能体框架能够有效应对大规模代码库中预取搜索空间广阔、预取共依赖与干扰等挑战，在性能提升上超越既有编译器与FDO启发式方法。

Abstract: Data prefetching is an established technique to mitigate cache miss latency and keep the processor saturated with data. Software exists in a unique position to issue prefetches, having algorithmic knowledge of the workload at hand. However, inserting software prefetches is a time consuming, unpredictable task, with a high degree of sensitivity to microarchitecture or future code changes. While several compiler- and FDO-based approaches insert and tune software prefetches automatically, they use heuristics that do not generalize and do not consider issues that arise in large codebases. In this paper, we show that semantic understanding and reasoning about a program is a vital component in inserting effective software prefetches. We also identify that prefetching at the scale of large codebases poses new challenges, including prefetch codependence and interference.
  To remedy this, we build Presage, a system that leverages the semantic reasoning of Large Language Models to insert effective software prefetches. Presage processes a workload through a custom agent harness specifically designed to navigate the wide space of potential prefetches on large-scale workloads. First, a proposer agent with access to performance metrics identifies promising software prefetching candidates. Then, several long-horizon optimizer agents optimize prefetching candidates in a loop, working towards discovery of performance wins. Finally, a combiner agent handles composition of individually effective prefetches. Through this method, Presage is able to insert prefetches that improve performance by a geomean of 10% across 81 workloads by exploring tens of different prefetching alternatives per workload, including a 2.7% runtime reduction on SPEC CPU 2026 where prior SOTA fails. When compared against prior SOTA on its evaluation suite, Presage achieves a geomean runtime improvement of 17% compared to SOTA's 10%.

</details>


### [6] [TEMPO: A Tag-Based Framework for Efficient Memory Ordering](https://arxiv.org/abs/2609.22743)
*Pranith Kumar,Prasun Gera,Hyojong Kim,Chulhyung Park,Hyesoon Kim*

Main category: cs.AR

TL;DR: 提出TEMPO，一种基于标签的框架，用于精确实现弱内存模型处理器的ordering指令，消除保守的退休串行化与投机load squash/replay。


<details>
  <summary>Details</summary>
Motivation: 弱内存处理器依赖ordering指令保证正确性，但传统实现往往比内存模型要求更保守，导致drain诱导的退休停顿以及投机load的保守squash/replay，抑制合法执行、降低吞吐。

Method: TEMPO为指令分配轻量ordering标签，将ordering强制分解为退休时谓词与完成时store ordering两部分，使核心在无需保守退休串行化的情况下强制所需ordering。

Result: 在原生四线程负载上，几何平均归一化执行周期降低7.9%；在跨ISA执行（如x86-on-Arm）的插桩SPEC2017 DBT代理上，几何平均IPC提升15.9%；每核仅增加262字节。

Conclusion: TEMPO通过标签化机制精确实现ordering指令，消除了不必要的退休串行化和投机load squash/replay，以极小的硬件开销显著提升性能。

Abstract: Weak-memory processors rely on ordering instruc- tions for correctness, yet conventional implementations often en- force them more conservatively than the memory model requires. This over-enforcement manifests as drain-induced retirement stalls at ordering instructions and conservative squash/replay of speculative loads, suppressing legal executions and reducing throughput. We present TEMPO, a tag-based framework for precise microarchitectural implementation of ordering instructions. TEMPO assigns lightweight ordering tags to instructions and decomposes enforcement across retirement-time predicates and completion-time store ordering, allowing the core to enforce required ordering without conservative retirement serialization. TEMPO eliminates unnecessary retirement serialization at ordering instructions and speculative-load squash/replay. In our evaluation, TEMPO reduces geometric-mean normalized exe- cution cycles by 7.9% on native four-thread workloads and improves geometric-mean IPC by 15.9% on an instrumented SPEC2017 dynamic binary translation (DBT) proxy for cross- ISA execution (e.g., x86-on-Arm), while adding only 262 bytes per core.

</details>


### [7] [Quality over Quantity: Diversity-Aware Data Selection for Efficient Verilog Code Generation](https://arxiv.org/abs/2609.22765)
*Yiheng Shen,Wei Zheng,Xiao Wei,Hao Shen,Xiang Chen,Guang Yang*

Main category: cs.AR

TL;DR: 提出VeriSelector，首个联合优化质量与多样性的Verilog代码生成数据选择框架，通过三阶段近似求解约束双目标子集选择问题，仅用20%-25%数据即超越全量训练与SOTA基线。


<details>
  <summary>Details</summary>
Motivation: 现有Verilog代码生成数据集存在大量噪声与冗余；已有数据选择方法仅关注单一质量维度，忽视训练集全局多样性，且无法捕捉Verilog特有的结构语义。

Method: 将数据选择建模为约束双目标子集选择问题并做三阶段近似求解。质量维度采用多粒度流水线：先通过testbench仿真验证功能正确性，再用指令遵循难度（IFD）评分过滤错配样本；多样性维度融合109维Verilog特有结构特征（AST、CFG、Netlist）与文本嵌入进行聚类建模；随后采用比例自适应采样策略，依据IFD排名分配各簇配额，并具备可证明的分布保持保证。

Result: 在3个LLM与3个基准上，仅用20%-25%数据即优于全量训练和SOTA基线，性能保持率超过118%，训练时间减少80%以上；相较全量训练平均Pass@1提升18.49%-29.43%，相较最优基线提升1.36%-7.95%。

Conclusion: VeriSelector验证了联合优化质量与多样性的Verilog专用数据选择的有效性，可在显著降低数据量与训练成本的同时提升代码生成性能。

Abstract: Large Language Models (LLMs) have shown remarkable potential in Verilog code generation, yet existing datasets contain con siderable noise and redundancy. Prior data selection methods address only isolated quality aspects, neglect the global diversity of the training set, and cannot capture Verilog-specific structural semantics. To bridge this gap, we propose VeriSelector, the first data selection framework for Verilog code generation that jointly optimizes quality and diversity. We formulate the selec tion problem as a constrained bi-objective subset selection problem and solve it via a three-stage approximation. For quality, a multi-granularity pipeline first verifies functional correctness through testbench simulation and then filters misaligned samples via Instruction-Following Difficulty (IFD) scoring. For diversity, 109-dimensional Verilog-specific structural features (AST, CFG, and Netlist) are fused with textual embeddings for clustering-based diversity modeling. A proportional adaptive sampling strat egy then allocates per-cluster quotas guided by IFD ranks, with a provable distribution preservation guarantee. Experiments on three LLMs and three benchmarks show that VeriSelector outperforms full-dataset training and state-of-the-art baselines us ing only 20%-25% of the data, achieving Performance Retention Rates above 118% and reducing training time by over 80%. Notably, VeriSelector improves average Pass@1 by 18.49%-29.43% over full-dataset training and by 1.36%-7.95% over the best-performing baseline across all evaluated models.

</details>


### [8] [Benchmarking StreamNTT with a Verilog-to-Routing Toolchain](https://arxiv.org/abs/2609.23116)
*Wei He,Young-kyu Choi,Hyunwoo Park,Sunwoong Kim*

Main category: cs.AR

TL;DR: 本文在开源 Verilog-to-Routing 工具链上复现了基于高层次综合与 FPGA 的 NTT 加速器 StreamNTT，DSP 与乘法器资源占用与商业工具方案相近，但内部存储器利用率差异显著，说明仍需内存级优化才能逼近商业工具性能。


<details>
  <summary>Details</summary>
Motivation: 后量子密码算法走向数据中心规模化部署，其计算瓶颈数论变换（NTT）的硬件加速受到关注。StreamNTT 虽取得领先吞吐，但依赖商业工具与特定器件，缺乏相应环境的研究者难以进行直接对比与复现。

Method: 将 StreamNTT 移植构建于开源 Verilog-to-Routing 工具链之上，并对其硬件资源占用（DSP、乘法器、内部存储器）进行对比分析。

Result: 开源工具链实现取得了与商业工具方案相近的数字信号处理单元与乘法器使用量；但在内部存储器利用率上存在显著差异，表明当前开源流程下的内存利用效率仍不及商业工具。

Conclusion: 开源工具链可用于复现 StreamNTT 并达到相近的逻辑资源效率，但内存层面的优化是缩小与商业工具性能差距的关键方向。

Abstract: As post-quantum cryptography algorithms move toward large-scale data center deployment, hardware acceleration of their computational bottleneck, which is the number theoretic transform (NTT), has gained increasing attention. StreamNTT, a high-level synthesis- and field-programmable gate array-based accelerator, achieves state-of-the-art throughput through various optimization techniques. However, its reliance on a commercial tool and a device makes direct comparisons difficult for researchers without access. We address this by building StreamNTT on an open-source Verilog-to-Routing toolchain, which achieves similar digital signal processing and multiplier usage. Significant differences in internal memory utilization indicate that further memory-level optimization is needed to approach commercial tool performance.

</details>


### [9] [WaveletECO: A Closed-Loop Physical ECO Platform and a Specialized Local Language Model](https://arxiv.org/abs/2609.23444)
*Guoxiang Xu,Guozhen Ji,Zijian Luo,Zhengrui Chen,Qi Sun,Cheng Zhuo*

Main category: cs.AR

TL;DR: 提出WaveletECO：闭环执行平台+LLM智能体实现ECO多轮修复决策，并用监督微调与CPO-SimPO训练本地9B模型，性能超越GPT-6 Astra且推理成本大幅降低。


<details>
  <summary>Details</summary>
Motivation: ECO（工程变更单）是芯片设计后期修复时序与电气违规的关键步骤。现有Agentic EDA方法主要聚焦于工具调用，对模型决策质量和针对性训练关注不足。ECO的核心难点在于多轮决策：模型需依据每轮执行结果决定下一步修复动作。

Method: 构建WaveletECO，将闭环执行平台与大型语言模型集成，使智能体能够有效执行ECO决策；利用执行演示与决策偏好数据，通过监督微调（SFT）和CPO-SimPO训练本地9B模型，实现本地部署下的ECO决策。

Result: 在22个设计的594次评估运行中，WaveletECO-Policy（BF16）与（INT8）得分分别为79.63和79.65，高于GPT-6 Astra的77.44；INT8推理成本约为GPT-6 Astra的1/147。

Conclusion: 针对性的专用模型训练可支撑高效、低成本的多轮ECO修复，且在INT8量化下修复质量得以保持。

Abstract: Engineering change order (ECO) is an important step in repairing timing and electrical violations during the late stages of chip design. Existing Agentic EDA methods primarily focus on tool invocation, with less attention to model decision quality and targeted training. A central challenge in ECO is multi-round decision-making: the model must use the results of each round to determine the next repair action. We propose WaveletECO, which integrates a closed-loop execution platform with large language models to enable agents to execute ECO decisions effectively. We also train a local 9B model through supervised fine-tuning and CPO-SimPO using execution demonstrations and decision-preference data, enabling ECO decision-making with a locally deployed model. Across 594 evaluation runs on 22 designs, WaveletECO-Policy (BF16) and (INT8) score 79.63 and 79.65, respectively, compared with GPT-6 Astra's 77.44. The estimated inference cost of INT8 is about 1/147 of GPT-6 Astra's. These results show that specialized model training supports effective, low-cost multi-round ECO repair, with repair quality retained under INT8 quantization.

</details>


### [10] [SPLASH: Co-Designing Sparse Attention with High-Bandwidth Flash for Efficient Long-Context Inference](https://arxiv.org/abs/2609.23816)
*Aditya Anirudh Jonnalagadda,Agasthi Haputhanthri,Pranav Dangi,Rohan Juneja,Wenshuo Yue,Aritra Bagchi,Bin Gao,Tulika Mitra*

Main category: cs.AR

TL;DR: SPLASH通过HBM-HBF分层虚拟化KV缓存，并将稀疏注意力与HBF的页粒度及平面级并行特性协同设计，在保持精度的同时大幅提升长上下文解码吞吐。


<details>
  <summary>Details</summary>
Motivation: 随着上下文长度、并发量和请求生命周期增长，KV缓存已成为LLM服务系统的主要内存消耗者。HBM带宽高但容量有限，片外内存与存储容量大但带宽不足以支撑注意力解码。HBF兼具TB级容量与接近HBM的读带宽，其写寿命有限，天然适合只读权重，作者进一步论证HBF与HBM组成分层结构也可承载KV缓存；与以往次级层存在带宽瓶颈的分层不同，两者带宽相当，可作为面向长上下文KV缓存的单一逻辑内存。同时，HBF按整页读取并通过数千个并行flash平面聚合带宽，稀疏注意力必须与这些物理特性协同设计。

Method: 提出SPLASH，一种算法与架构协同设计方案：在HBM与HBF之间虚拟化KV缓存，并按HBF的页粒度（flash page）和平面级并行（plane-level parallelism）调整稀疏注意力机制。

Result: 在多种模型和上下文长度下，以每token 100 ms延迟为目标，SPLASH相比所评估基线将单GPU解码吞吐提升3.5倍至11.4倍，同时在长上下文测试集上精度与稠密注意力差距控制在4%以内。

Conclusion: 将HBF与HBM构成带宽相当的分层存储、并以虚拟化KV缓存配合HBF感知的稀疏注意力，可有效解决长上下文LLM服务的容量与带宽矛盾，在延迟约束下显著提升吞吐且精度损失有限。

Abstract: The key-value (KV) cache has become the dominant consumer of memory in large language model (LLM) serving systems as context lengths, concurrency, and request lifetimes grow. High-bandwidth memory (HBM) provides the bandwidth attention decode needs but limited capacity, while off-package memory and storage add capacity but lack the bandwidth to sustain attention decode. High-Bandwidth Flash (HBF) is a promising substrate that combines terabyte-scale capacity with near-HBM read bandwidth. Limited write endurance makes read-only model weights its natural use, but we argue that HBF paired with HBM as a hierarchy can also hold the KV cache. Unlike prior hierarchies, whose secondary tiers are bandwidth bottlenecked, the comparable bandwidths let the two act as one logical memory for the long-context KV cache. HBF capacity enables long-context serving, and sparse attention makes it efficient by limiting KV-cache reads during memory-bound decode. Since HBF reads full flash pages and aggregates bandwidth by accessing thousands of parallel flash planes, sparse attention must be co-designed with these physical properties. We present SPLASH, an algorithm and architecture co-design that virtualizes the KV cache across HBM and HBF and adapts sparse attention to HBF's page granularity and plane-level parallelism. Across models and context lengths, SPLASH improves decode throughput per GPU by 3.5x-11.4x over the evaluated baselines under a 100 ms per-token latency objective, while keeping accuracy within 4% of dense attention across long-context suites.

</details>


### [11] [Dissecting How Die Scaling Breaks GPU Fine-grained Scheduling](https://arxiv.org/abs/2609.24270)
*Xiaoze Fan,Jianhao Wang,Weihao Cui,Han Zhao,Zhuobin Huang,Yangjie Zhou,Yuxian Qiu,Shixuan Sun,Bingsheng He,Quan Chen,Minyi Guo*

Main category: cs.AR

TL;DR: 该论文揭示现代GPU的物理非对称性（制造缺陷导致的拓扑差异与缓存/内存分区导致的非均匀访存），提出轻量级刻画方法识别每芯片的计算拓扑与内存亲和性，并将该信息融入细粒度调度，实现非对称感知的资源分配，提升主流内核与LLM推理性能。


<details>
  <summary>Details</summary>
Motivation: 现代GPU在物理层面已不再对称：工艺微缩同时带来制造驱动的"降级筛选"（floorsweeping）和缓存/内存分区。前者造成芯片间计算拓扑差异，后者造成非均匀内存访问（NUMA-like）。这些非对称性影响显著——忽略拓扑的计算单元分配可导致高达1.33倍的性能波动，远程访问使HBM延迟上升67%、L2延迟近乎翻倍。然而这些差异被GPU的逻辑资源抽象所掩盖，且随芯片不同而变化，现有调度器无法感知。

Method: 作者开发了轻量级的芯片刻画方法，用于揭示每颗芯片的实际计算拓扑（物理计算单元布局）与内存亲和性（哪些SM与哪些缓存/内存分区就近）。随后将所发现的信息注入现有细粒度调度器，使其具备非对称感知能力：调度决策不仅考虑"分配多少资源"，还考虑"分配哪些物理资源"。

Result: 在三个场景中验证效果：全GPU内核执行、应用内多路复用（multiplexing）以及应用间共置（co-location）。非对称感知调度使主流内核性能最高提升1.22倍；多路复用的LLM推理最高提升14.3%；并消除了最高达1.33倍的性能波动。

Conclusion: GPU的物理非对称性是被逻辑抽象隐藏但普遍存在且影响重大的实际问题。通过轻量刻画将其显式暴露，并让调度具备物理资源位置感知能力，可在不改变硬件的前提下显著提升性能并降低芯片间性能差异，对内核调度、多租户共置和LLM推理服务等场景均有实际价值。

Abstract: Modern GPUs are no longer physically symmetric. Die scaling leads to both manufacturing-driven floorsweeping and cache and memory partitioning. The former creates chip-specific compute topologies, while the latter causes non-uniform memory access. These asymmetries are substantial. Topology-oblivious compute unit allocation can lead to up to 1.33x performance variation, while remote accesses increase HBM latency by up to 67% and nearly double L2 latency.
  However, these asymmetries are hidden behind the GPU's logical resource abstractions and can vary across chips. We develop lightweight characterization methods to uncover per-chip compute topology and memory affinity. We then use the discovered information to make existing fine-grained scheduling asymmetry-aware, considering not only how many resources are allocated but also which physical resources are assigned. Across full-GPU kernel execution, intra-application multiplexing, and inter-application co-location, asymmetry-aware scheduling improves mainstream kernels by up to 1.22x, multiplexed LLM inference by up to 14.3%, and avoids up to 1.33x performance variation.

</details>


### [12] [Circuit-Architecture-Training Co-Design with Regenerative-SA Similarity Sensing for Aggressive SAR Skipping in Analog Compute-in-Memory](https://arxiv.org/abs/2609.24288)
*Yufei Liu,Shuang Liu,Junjie Wang*

Main category: cs.AR

TL;DR: 提出电路-架构-训练协同设计框架，利用感测放大器再生检测模拟输出相似性，减少CIM中SAR比较次数，实现精度近无损的高能效。


<details>
  <summary>Details</summary>
Motivation: 降低存内计算（CIM）系统中SAR ADC的比较次数和参考ADC能耗，同时尽量保持模型精度。

Method: 面向硬件的训练，建模电路表征的感测放大器扰动及前缀复用编码误差，使比较跳过更激进；检测器通过55nm CMOS原理仿真表征，系统级在ISAAC-based W4A4 CIM模型上评估WRN-28-10、ResNet20与DeiT。

Result: WRN-28-10上Top-1精度77.3%（W4A4基线78.4%），评估层SAR比较减少48.19%；扣除检测器开销后参考ADC能耗降低27.18%，每次转换剩余0.52 pJ用于控制与外围成本。

Conclusion: 该协同设计在精度近无损的前提下显著减少SAR比较与参考ADC能耗，验证感测放大器再生检测用于模拟输出相似性识别的可行性。

Abstract: This work presents a circuit-architecture-training co-design framework that exploits sense-amplifier (SA) regeneration to detect analog-output similarity and reduce SAR comparisons in compute-in-memory (CIM) systems. Hardware-aware training incorporates circuit-characterized SA disturbance and encoding errors caused by prefix reuse, enabling aggressive comparison skipping. The detector is characterized through 55-nm CMOS schematic simulations, with system-level evaluation on WRN-28-10, ResNet20, and DeiT using an ISAAC-based W4A4 CIM model. On WRN-28-10, the proposed approach achieves 77.3% Top-1 accuracy (W4A4 baseline: 78.4%) while reducing SAR comparisons by 48.19% across the evaluated layers. Energy-budget analysis estimates a 27.18% reduction in reference ADC energy after detector overhead, leaving 0.52 pJ per conversion to accommodate additional control and peripheral costs.

</details>


### [13] [NPU Accelerator: Quantized Real-Time Vehicle Detection on PYNQ-Z1 Using FINN](https://arxiv.org/abs/2609.24757)
*Daniel Gutierrez,Antonio Cuesta,Jorge Fe,Bruno Gutierrez,Rashed Al Koutayni*

Main category: cs.AR

TL;DR: 本文在PYNQ-Z1（Zynq XC7Z020）上实现了面向实时车辆检测的NPU加速器，采用QAT+轻量YOLO+FINN的软硬件协同设计，最优配置以35.66 FPS、12.25 FPS/W、45.11 ms延迟和0.594 mAP@0.5同时满足全部四项指标。


<details>
  <summary>Details</summary>
Motivation: 在资源受限的小型Zynq FPGA上实现实时车辆检测面临算力、功耗与延迟的多重约束，需要同时满足吞吐率>30 FPS、能效>7 FPS/W、PL延迟<50 ms、VOC精度>0.55 mAP@0.5四项工程要求。

Method: 采用软硬件协同设计流程：量化感知训练（QAT）、轻量YOLO衍生检测器、Brevitas/QONNX模型导出、FINN数据流编译、Vivado实现与板级物理测试。设计空间涵盖LP-YOLO与LP-YOLO Slim变体、自定义YOLOv3-tiny基线、4比特与混合低位量化、320×320与256×256输入、手动/自动FIFO尺寸以及100–200 MHz PL时钟。

Result: 最终LP-YOLO Slim配置采用256×256输入、w2a4量化与142.86 MHz PL时钟；batch=100时达到35.66 FPS、2.91 W，即12.25 FPS/W，实测PL延迟45.11 ms，VOC mAP@0.5为0.594。这是所有评测配置中唯一同时满足四项要求的方案。

Conclusion: 低位QAT、架构瘦身、FINN折叠与FIFO优化以及适度时钟提升相结合，可在小型Zynq FPGA上提供实用的实时检测器。

Abstract: This paper presents the design, optimization, implementation, and on-board validation of a neural processing unit (NPU) accelerator for real-time vehicle detection on the resource-constrained Xilinx Zynq XC7Z020 device of the PYNQ-Z1 board. The work follows a hardware/software co-design methodology that combines quantization-aware training (QAT), lightweight YOLO-derived detectors, Brevitas/QONNX model export, FINN dataflow compilation, Vivado implementation, and physical benchmarking on the target board. Four simultaneous engineering requirements define successful deployment: throughput above 30 frames/s (FPS), energy efficiency above 7 FPS/W, programmable-logic (PL) hardware latency below 50 ms, and Pascal VOC detection accuracy above 0.55 mAP@0.5. The design space includes LP-YOLO and LP-YOLO Slim variants, a custom YOLOv3-tiny reference, 4-bit and mixed low-bit quantization, 320$\times$320 and 256$\times$256 inputs, manual and automatic FIFO sizing, and programmable-logic clocks from 100 to 200 MHz. The final LP-YOLO Slim configuration uses a 256$\times$256 input, w2a4 quantization, and a 142.86 MHz PL clock. With batch 100 it reaches 35.66 FPS at 2.91 W, corresponding to 12.25 FPS/W, while measured PL latency is 45.11 ms and VOC mAP@0.5 is 0.594. This is the only evaluated configuration for which the supplied measurements satisfy all four requirements simultaneously. The results show that low-bit QAT, architectural slimming, FINN folding and FIFO optimization, and moderate clock scaling can jointly provide a practical real-time detector on a small Zynq FPGA.

</details>


### [14] [SPECTRA: Adaptive Execution of Speculative Decoding on a Runtime-Reconfigurable Tiled Architecture](https://arxiv.org/abs/2609.24847)
*Gabriele Tombesi,William Baisi,Je Yang,Elisavet Lydia Alvanaki,Kevin Lee,Michael Lippe,Biruk Seyoum,Luca P. Carloni*

Main category: cs.AR

TL;DR: SPECTRA 是一种运行时可重构的分块架构，通过在算子（kernel）粒度上在脉动阵列（GEMM）与向量通道（GEMV）执行间切换，并动态调整跨分块并行度，统一覆盖推测解码全流水线，在 20 分块 FPGA 原型上相比固定设计最高取得 2.09×（分块级）+1.25×（系统级）加速。


<details>
  <summary>Details</summary>
Motivation: 边缘设备上的 LLM 推理受计算与内存资源限制，自回归解码效率低。推测解码通过小模型草拟、目标模型批量并行验证来缓解瓶颈，但验证阶段的计算强度随推测长度与接受率变化，处于解码阶段内存受限的 GEMV 与预填充阶段计算受限的 GEMM 之间的运行态中间区间。单一固定架构难以在整条流水线上维持高利用率。

Method: 提出 SPECTRA 运行时可重构分块架构。分块内：计算引擎在面向 GEMM 的脉动执行与面向 GEMV 的向量通道执行之间切换。分块间：动态选择分块数量、算子分区方式与通信模式，以调整计算并行度。分块级与系统级重构均以逐 kernel 为粒度进行，从而适配多样化的计算区间。

Result: 在 20 分块 FPGA 原型上，针对 Pythia、SmolLM2 与 GPT-2 系列进行评测，分块级重构相比固定设计最高提速 2.09×，系统级自适应进一步带来 1.25× 增益。

Conclusion: 逐 kernel 的运行时可重构性（分块内执行模式切换 + 分块间并行度自适应）能有效覆盖推测解码中从 GEMV 到 GEMM 的连续计算区间，是边缘端加速推测解码的可行硬件方案。

Abstract: LLM inference on edge devices is constrained by computational and memory resources, making efficient autoregressive decoding challenging. Speculative decoding alleviates this bottleneck by generating tokens with a smaller draft model and verifying multiple tokens in parallel with a batched target model pass. However, verification introduces a runtime-dependent intermediate regime between memory-bound general matrix-vector (GEMV) operations in decoding and compute-bound general matrix-matrix (GEMM) operations in prefill, as its arithmetic intensity varies with speculation length and acceptance rate. We present SPECTRA, a runtime-reconfigurable tiled architecture that sustains high utilization across the full speculative decoding pipeline. Within each tile, the compute engine switches between systolic execution for GEMMs and vector-lane execution for GEMVs. Across tiles, SPECTRA dynamically adapts computation parallelism by selecting tile count, kernel partitioning, and communication pattern. Both tile-level and system-level reconfiguration operate on a per-kernel basis, enabling efficient execution across these diverse regimes. Evaluated on a 20-tile FPGA prototype across the Pythia, SmolLM2, and GPT-2 families, SPECTRA achieves up to $2.09\times$ speedup from tile-level reconfiguration and a further $1.25\times$ gain from system-level adaptability over fixed designs.

</details>


### [15] [Toward Multi-kW Power Delivery Methodologies for Advanced 3D Heterogeneous Integration](https://arxiv.org/abs/2609.24904)
*Peiyi Yue,Hangyu Zhang,Ratul Das,Ramesh Harjani,Sachin S. Sapatnekar*

Main category: cs.AR

TL;DR: 本文综述面向超大规模异构集成系统（含2D/3D芯粒先进封装）的多级分布式供电（power delivery）设计方法论，以在高功率密度与引脚瓶颈下兼顾性能、热与可靠性约束。


<details>
  <summary>Details</summary>
Motivation: 现代应用（尤以AI应用为代表）推动系统规模持续膨胀，晶体管数可能突破万亿量级；此类系统需借助先进封装将异质2D/3D芯粒置于基板上，功耗极大，高功率密度与引脚数瓶颈使可靠稳健的供电成为重大挑战。

Method: 论文采用综述/概览方式，梳理构建供电设计方法论的技术路径：设计多级分布式供电系统，并针对性能与可靠性进行优化，使其能在严苛的性能、热与可靠性约束下共存。

Result: 摘要未给出具体实验数据或量化结果，主要贡献在于对克服供电瓶颈的设计方法学途径进行系统概述与总结。

Conclusion: 应对超大规模异构集成系统的供电难题，需要发展多级分布式供电架构及其配套设计方法学，在性能、热与可靠性约束间取得协同优化。

Abstract: The demands of modern applications require the construction of ever more complex integrated systems, with AI applications in particular serving as a significant driver for increased system size, potentially going beyond the trilliontransistor mark. The path to building these systems requires the use of advanced packaging, with heterogeneous integrated 2D and 3D chiplets placed atop a substrate. Such computationally powerful systems require significant power for computation: reliable and robust power delivery is a major challenge in light of high power densities and pin count bottlenecks. This paper overviews approaches to building design methodologies that overcome this problem, through the design of multistage distributed power delivery systems, optimized for performance and reliability, and built to coexist within stringent performance, thermal, and reliability constraints.

</details>
